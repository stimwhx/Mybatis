<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userLogoinmapper">
    <select id="selectAll" resultType="com.oneobject.modle.Whx" parameterType="int">
        select * from whx
    </select>
    <insert id="insetData" parameterType="com.oneobject.modle.Whx">
<!--这不会-->
    </insert>
</mapper>